{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "codegen": {
      "inputs": [
        "src/templates/*.ohm"
      ],
      "outputs": [
        "src/templates/*.ohm-bundle.*"
      ]
    }
  }
}
