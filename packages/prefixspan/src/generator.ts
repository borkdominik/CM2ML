import type { Matches, Occurs, Pattern } from './types'

export function isGenerator(_input: number[][], _patt: Pattern, _matches: Matches) {
  // TODO
  return false
  // const last = patt.at(-1)!
  // const lastMatches = matches.filter(([i, _]) => input[i]!.at(-1)! === last)
  // return lastMatches.length === matches.length
}

export function canGeneratorPrune(_input: number[][], _patt: Pattern, _matches: Matches, _occurrsStack: Occurs[]) {
  // TODO
  return false
}
